// 1. Declara una variable de tipo string llamada "animal" que contenga el valor "alpaca"
var animal = 'alpaca';

// 2. Declara una variable de tipo number llamada "books" que cotenga el valor 57
var books = 57;

// 3. Declara una variable de tipo boolean llamada "isValid" que cotenga el valor true
var isValid = true;

// 4. Declara una variable de tipo array llamada "allAnimals" que cotenga los valores: "alpaca", "cebra", "rana"
var allAnimals = ['alpaca', 'cebra', 'rana'];

// 5. Declara una variable de tipo object llamada "superPerson" que contenga las claves/valor:
// firstName => Pedro (tipo string)
// lastName => Parques (tipo string)
// age => 28 (tipo number)
var superPerson = {
  firstName: 'Pedro',
  lastName: 'Parques',
  age: 28
};

// ⬇️ NO TOCAR ESTA PARTE O ROMPERÁS LOS TESTS ⬇️
export default { animal, books, isValid, allAnimals, superPerson };
